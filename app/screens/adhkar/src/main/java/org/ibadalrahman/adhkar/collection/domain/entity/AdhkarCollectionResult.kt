package org.ibadalrahman.adhkar.collection.domain.entity

/**
 * The collection list produces no domain results — see [AdhkarCollectionAction]. This type exists
 * only to satisfy the MVI base class's type parameters.
 */
sealed interface AdhkarCollectionResult
