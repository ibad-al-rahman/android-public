package org.ibadalrahman.settings.notifications.domain.entity

/**
 * No domain actions yet — all intentions map straight to state results because nothing is
 * persisted. When persistence lands, save actions go here.
 */
sealed interface NotificationsAction
